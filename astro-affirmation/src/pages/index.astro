---
import Layout from "../layouts/Layout.astro";
import PostDisplay from "../components/PostDisplay.astro";
import { loadAllPosts } from "../utils/posts";

const allPosts = await loadAllPosts();
---

<Layout>
  <PostDisplay posts={allPosts} />
</Layout>
