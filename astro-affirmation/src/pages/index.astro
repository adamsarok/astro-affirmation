---
import { getCollection } from 'astro:content';

const posts = await getCollection('posts');
const randomPost = posts[Math.floor(Math.random() * posts.length)];
const randomSlug = randomPost.slug;

return Astro.redirect(`/${randomSlug}`);
---
